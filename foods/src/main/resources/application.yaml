spring:
  application:
    name: foods

  datasource:
    url: jdbc:mysql://localhost:3306/practice
    username: root
    password: hrishi@sql
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
